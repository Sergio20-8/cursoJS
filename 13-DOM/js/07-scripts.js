// Cambiando la apariencia del css

const elemento = document.querySelector('.contenido-hero'); 
console.log(elemento.children);


//Sirve para eliminar un elemento
// elemento.removeChild(elemento.children[0]);