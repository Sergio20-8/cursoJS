const producto = {
    nombre: 'Monitor 20 pulgadas',
    precio: 300,
    disponible: true
} 

//console.log(producto.toUpperCase()); // Error, los objetos no tienen el metodo toUpperCase
console.log(producto.nombre.toUpperCase());
console.log(producto.nombre);